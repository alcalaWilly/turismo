import 'package:flutter/material.dart';
import 'package:turismo/src/pages/home/modules/categoriaModel/categorias.dart';

class Rtecnicas extends StatelessWidget {
  const Rtecnicas({super.key, required Categorias categoria});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('REALIZACIONES TÉCNCIAS'),
    );
  }
}
